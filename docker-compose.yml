docker-compose.yml:
services: 
  app: 
    build: 
    . container_name: ids-app 
    network_mode: "host" 
    cap_add: 
      - NET_ADMIN 
      - NET_RAW 
    stdin_open: true 
    tty: true 
    ports: 
      - "5000:5000" 
    environment: 
      - PYTHONUNBUFFERED=1
